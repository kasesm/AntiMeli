name: Run Telegram Config Scraper

on:
  schedule:
    # اجرای خودکار هر ۶ ساعت یک‌بار (می‌توانی زمانش را تغییر دهی)
    - cron: '0 */6 * * *'
  workflow_dispatch:
    # فعال کردن دکمه اجرای دستی در تب Actions گیت‌هاب

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install telethon pycryptodome

      - name: Run Scraper Script
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
        run: python scraper.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add configs.txt
          # کامیت کردن تغییرات فقط در صورتی که فایل جدیدی اضافه شده باشد تا ارور ندهد
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update collected configs [Automated]" && git push)
